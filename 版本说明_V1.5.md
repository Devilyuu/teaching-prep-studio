# 版本说明 V1.5.0

变更日期：2026-09-20
变更来源：教师在 GPT6 环境用 V1.4.1 实测后的反馈。

## 一句话说明

**课件生成器从"临场判断推荐项"改为"固定默认优先级"：专业课件生成器（PPT Master 类）优先，脚本直接生成PPTX降为仅在教师明确要求时使用；环境里没有专业生成器时打包交接，不再就地退回脚本凑交付。**

## 为什么改

教师实测 V1.4.1 的脚本方式产出后的结论（如实记录）：

- **视觉观感单薄**：配色平、排版死板、缺图形与层次，成品像文字稿而不像课件；
- **图片处理差**：拉伸变形、裁切不当、占位图残留、图文不对应。

V1.4.1 把脚本方式与专业生成器并列为对等选项，推荐项靠临场判断"选择维度"得出。实测表明这个判断不可靠——脚本方式在版式精确性上确实有优势，但那不是课堂课件的主要质量瓶颈，视觉表现才是。

## 具体改了什么

| 文件 | 改动 |
|---|---|
| `SKILL.md` | 阶段7 核心规则改为"优先专业生成器 + 教师点选确认"两条；新增 D 段"无专业生成器则打包交接"；新增脚本升级提示；原则7、阶段导引第8条、可插拔原则同步调和；version 1.4.1 → 1.5.0 |
| `参考规则/06_课件生成器路由.md` | 新增「默认优先级」表与「为什么脚本方式降到最后」；新增「不要就地降级」；新增「视觉质量红线」；选择顺序与选项示例改写 |
| `参考规则/05_跨Agent一次性交接协议.md` | 从只讲视觉素材扩展为两类交接；新增「从无专业课件生成器的Agent交给有 PPT Master 的Agent」完整流程 |
| `参考规则/08_质量评分标准.md` | 一票否决项新增4条：无专业生成器却退回脚本凑交付、图片变形/占位图未替换、整页平铺文字无层次 |
| `模板/课件生成器任务书.md` | 强化为可直接交接的任务包说明书；新增交接场景、视觉质量红线7条、回传要求 |
| `模板/生成结果验收清单.md` | 原清单归为「视觉素材完成包验收」；新增「课件完成包验收」12项 |
| `模板/课程总控档案.yaml` | `课件生成偏好` 补默认优先级注释与脚本升级提示说明 |
| `适配器/课件生成器适配规范.md` | 新增「接入后的优先级位置」：按能力判定档位，不能可靠处理本地图片的不得作推荐项 |

## 没有改什么（重要）

- **"教师点选一次"的原则保留。** 优先级只决定选项排序和推荐项，不代替教师拍板。V1.4.0 确立的"不静默代选"仍然有效。
- **脚本方式没有删除。** 它的原有优点（版式精确、文件小、可重复构建）依然成立，教师说"用脚本"时正常使用，只是不再是默认项。
- **可插拔架构没有动。** `03_课件生产清单.yaml` 仍是中立接口，新生成器按适配规范接入即可进入第1档排序。默认优先级不等于写死某个工具。

## 升级后对教师的实际影响

- 在有 PPT Master 的环境（如 Claude Code）：弹选项时 PPT Master 排第一并标注推荐，选了就用。
- 在没有专业生成器的环境（如 GPT6、Codex）：不再直接给一份脚本课件，而是打包《课件制作交接任务包》，教师整体转交一次，说一句"按这个课件任务包，用 PPT Master 把课件一次性做完并打包"，回来说"课件做好了，继续"。
- 档案里之前记了脚本方式的课程：下次进入课件阶段会被提示一次是否换到 PPT Master，确认后更新档案。
