# 全局安装说明

本 Skill 的文件夹名建议保持：

`teaching-prep-studio`

## Codex 全局安装

放到：

```text
~/.agents/skills/teaching-prep-studio/
```

Windows 通常对应：

```text
%USERPROFILE%\.agents\skills\teaching-prep-studio\
```

## Claude Code 全局安装

放到：

```text
~/.claude/skills/teaching-prep-studio/
```

Windows 通常对应：

```text
%USERPROFILE%\.claude\skills\teaching-prep-studio\
```

安装后重新启动对应Agent即可。

## 其他Agent

如果支持自定义 Skill / Rules / Agent Instructions：

- 优先将整个 `teaching-prep-studio` 文件夹作为全局 Skill 安装；
- 若只支持单文件规则，至少加载 `SKILL.md`；
- 若支持工作区级规则，也可把本 Skill 作为全局基础规则，再在课程工作区放课程材料。

如果某Agent没有生图能力，会自动降级为“一次性视觉素材任务包”模式；界面、图表类素材在有无头浏览器（如 Playwright + 本机 Edge/Chrome）的环境里可直接程序化渲染，不需要生图能力。
